# /usr/data/anvil/anvil.conf -- runtime switches. Edit on the printer, reboot.
# PRESERVED across mod updates.

# Web stack: nginx (Mainsail on :80) + moonraker (:7125).
# The only one there is -- FlashForge's HTTP API went with firmwareExe.
# 0 = no web UI at all, ssh only.
MOD_WEB=1

# Camera: mjpg-streamer on :8080, proxied by nginx at /webcam/. The binary is
# FlashForge's own, already on the firmware partition; firmwareExe used to be
# what started it. Optional tunables, defaults shown:
#MOD_CAM_RES=1280x720
#MOD_CAM_FPS=30
#MOD_CAM_PORT=8080
MOD_CAM=1

# Boot splash: a line of text on the panel from the moment the mod gets
# control until HelixScreen paints. Without it the screen is black for the
# whole of boot, every boot. FlashForge's own /usr/prog/start.img cannot fill
# that gap -- the only two lines that would display it, both in
# app_startup.sh, are commented out on this firmware. 0 = leave it black.
MOD_SPLASH=1

# Startup: bin/ff-startup.py, run by the firmwareExe wrapper just before the
# UI. EVERY boot it hands the toolhead boards over from their bootloaders,
# starts klipper, and waits for klipper + moonraker to be ready -- naming on
# the panel whichever board or service is holding things up, and handing the
# boards over again on each retry, which is what gives one that missed its
# window another chance. MOD_KLIPPER_TRIES (below) bounds the retries.
#
# 0 = hand klipper's start back to init.d/S70klipper, which does it the old
# way (background, printer.log tailing, no screen) and starts the UI at once.
# That is also what happens automatically if this script or the interpreter
# is missing -- klipper starting must never depend on python.
#MOD_STARTUP_TIMEOUT=240
MOD_STARTUP=1

# First-boot migration, done by the same program once the printer is up: it
# runs FF_IMPORT_FIRMWARE_CONFIG and SAVE_CONFIG to move this unit's factory
# calibration into printer.cfg, then stamps .firmware-config-imported so it
# never runs again. 0 = never migrate (you calibrate by hand); the startup
# wait above still happens.
MOD_IMPORT=1

# Touchscreen UI: HelixScreen, started by the firmwareExe wrapper.
# 0 = boot headless (blank screen); ssh and Mainsail are unaffected.
MOD_UI=1

# SSH: dropbear on :22. NOT read on the printer -- ssh comes from the stock
# /etc/init.d/S50dropbear and is up regardless. This is here because the value
# is baked in at build time (it decides whether a root password was set);
# editing it now changes nothing.
MOD_SSH=1

# WiFi: bring up wlan0 + wpa_supplicant + udhcpc, because firmwareExe used to
# do it and no longer runs.
MOD_WIFI=1

# Process supervision: s6-svscan, started first by init.d/S40s6 and watching
# $MODDIR/etc/s6 for service directories. That scandir is EMPTY today -- no
# service has been moved onto it yet -- so the scanner costs one small idle
# process and turning it off changes nothing you can see. It is here so that
# a printer where the supervisor itself is suspect has a switch, and so that
# the switch already exists on the day services depend on it. 0 = no scanner.
MOD_S6=1
