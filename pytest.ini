# Pins the rootdir. Without it, `pytest ./qa/replica` would take that directory
# as the root and never load qa/conftest.py, so the markers and the shared
# fixtures would vanish depending on whether you named the subdirectory or not.
[pytest]
testpaths = qa
