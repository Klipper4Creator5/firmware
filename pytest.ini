# Pins the rootdir. Without it, `pytest ./test/repo` would take that directory
# as the root and never load test/conftest.py, so the fixtures the two lanes
# share would vanish depending on which lane you named on the command line.
[pytest]
testpaths = test
