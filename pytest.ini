# Pins the rootdir. Without it, `pytest ./test/integration` would take that
# directory as the root and never load test/conftest.py, so the shared fixtures
# would vanish depending on whether you named the subdirectory or not.
[pytest]
testpaths = test
